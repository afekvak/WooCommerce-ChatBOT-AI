// src/mcp/prompts/index.ts
import { z } from "zod" // יבוא zod לאימות קלט
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js" // טיפוס שרת MCP

// פונקציה שרושמת את כל הפרומפטים (prompts) על השרת // הסבר
export function setupPrompts(server: McpServer) { // ייצוא פונקציה לשימוש מהשרת
 
} 
