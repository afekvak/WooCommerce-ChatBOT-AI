// src/mcp/resources/index.ts
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js" // טיפוס שרת MCP

// פונקציה שרושמת את כל המשאבים (resources) על השרת // הסבר
export function setupResources(server: McpServer) { 
 
} 
